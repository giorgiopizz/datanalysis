#include "datanalyzer.h"




int main(){
        //datanalyzer con più files e grafici
        vector<string> files;
        vector<string> names;
        dati= new datanalyzer(files,names, "counts");
        vector<*TF1> funzioni;

        vector::iterator() t=funzioni.begin();
        while(t!=funzioni.end()){
                //settare tutti i paramentri della funzione
                t++;
        }
        dati->fit(funzioni);
        dati->display();
        //dati->print();
        //datanalyzer con un solo file
        /*string file;
        string name;
        dati= new datanalyzer(file,name, "counts");
        *TF1 funzione;
        //settare tutti i paramentri della funzione
        dati->fit(funzione);
        dati->display();
        //dati->print();*/
}
